# Template — rendered to alertmanager.yml by scripts/deploy.sh (envsubst).
# The rendered alertmanager.yml contains secrets and is git-ignored.
global:
  smtp_smarthost: '${SMTP_SMARTHOST}'
  smtp_from: '${SMTP_FROM}'
  smtp_auth_username: '${SMTP_USER}'
  smtp_auth_password: '${SMTP_PASSWORD}'

route:
  receiver: email
  group_by: ['alertname', 'job']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    # Critical alerts go to email AND chat; warnings go to email only.
    - matchers:
        - severity="critical"
      receiver: email-and-chat

receivers:
  - name: email
    email_configs:
      - to: '${ALERT_EMAIL_TO}'
        send_resolved: true

  - name: email-and-chat
    email_configs:
      - to: '${ALERT_EMAIL_TO}'
        send_resolved: true
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL}'
        channel: '${SLACK_CHANNEL}'
        send_resolved: true
        title: '{{ .CommonLabels.alertname }} ({{ .CommonLabels.severity }})'
        text: "{{ range .Alerts }}{{ .Annotations.summary }}\n{{ end }}"
